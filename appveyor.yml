image: ubuntu1804

build: off

build_script:
  - pip install --user -U setuptools wheel
  - python3 setup.py build

test_script:
  - python3 setup.py flake8

after_test:
  - python3 setup.py bdist_wheel bdist_egg

artifacts:
  - path: dist/*

cache:
  - ${HOME}/.eggs -> setup.py
